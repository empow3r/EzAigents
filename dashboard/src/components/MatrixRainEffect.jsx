import React from 'react';

const MatrixRainEffect = ({ children, darkMode, intensity, showInteractiveElements }) => {
  return <>{children}</>; // Pass through children for now
};

export default MatrixRainEffect;